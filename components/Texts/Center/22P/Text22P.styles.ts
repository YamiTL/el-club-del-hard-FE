import { style } from 'typestyle';

const styles = {
  text: style({
    fontSize: '22px',
    fontFamily: '',
    fontWeight: '400',
    lineHeight: '28px',
    textAlign: 'center',
    color: '#000000',
  }),
};

export default styles;
