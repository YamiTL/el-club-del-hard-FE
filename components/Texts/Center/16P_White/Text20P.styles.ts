import { style } from 'typestyle';

const styles = {
  text: style({
    fontSize: '16px',
    fontFamily: '',
    fontWeight: '400',
    lineHeight: '23px',
    textAlign: 'center',
    color: '#FFFFFF',
  }),
};

export default styles;
