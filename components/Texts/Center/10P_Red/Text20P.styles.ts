import { style } from 'typestyle';

const styles = {
  text: style({
    fontSize: '10px',
    fontFamily: '',
    fontWeight: '400',
    lineHeight: '23px',
    textAlign: 'center',
    color: '#FF0000',
  }),
};

export default styles;
