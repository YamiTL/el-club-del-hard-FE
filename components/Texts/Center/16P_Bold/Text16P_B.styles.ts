import { style } from 'typestyle';

const styles = {
  text: style({
    fontSize: '16px',
    fontFamily: '',
    fontWeight: '700',
    lineHeight: '23px',
    textAlign: 'center',
    color: '#000000',
  }),
};

export default styles;
