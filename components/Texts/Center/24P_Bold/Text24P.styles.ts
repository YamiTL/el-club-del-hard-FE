import { style } from 'typestyle';

const styles = {
  text: style({
    fontSize: '24px',
    fontFamily: '',
    fontWeight: '700',
    lineHeight: '28px',
    textAlign: 'center',
    color: '#000000',
  }),
};

export default styles;
