import { style } from 'typestyle';

const styles = {
  text: style({
    fontSize: '24px',
    fontFamily: '',
    fontWeight: '400',
    lineHeight: '28px',
    textAlign: 'left',
    color: "#FFFFFF",
  }),
};

export default styles;
