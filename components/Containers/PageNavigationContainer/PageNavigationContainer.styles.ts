import {style} from 'typestyle';

const styles = {
    container: style({
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'flex-end',
        padding: '20px 80px',
        gap: '10px',
        width: '100%',
        height: '80px',
        backgroundColor: '#FFFFFF',
    })
}

export default styles;