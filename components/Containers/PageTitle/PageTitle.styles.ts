import {style} from 'typestyle';

const styles = {
    container: style({
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        padding: '40px 10px',
        gap: '10px',
        width: '100%',
        height: '127px',
    })
}

export default styles;