import {style} from 'typestyle';

const styles = {
    container: style({
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'center',
        padding: '10px',
        gap: '40px',
        width: '150px',
        height: '60px',
        backgroundColor: '#FFFFFF',
    }),
}

export default styles;