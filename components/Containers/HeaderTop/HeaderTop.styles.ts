import {style} from 'typestyle';

const styles = {
    container: style({
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'space-around',
        alignItems: 'center',
        padding: '10px 200px',
        gap: '90px',
        width: '100%',
        height: '240px',
        backgroundColor: '#FFFFFF',
    })
}

export default styles;