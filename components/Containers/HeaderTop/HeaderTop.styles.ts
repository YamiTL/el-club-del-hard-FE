import {style} from 'typestyle';

const styles = {
    container: style({
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'space-around',
        alignItems: 'center',
        padding: '10px 50px',
        gap: '150px',
        width: '100%',
        height: '150px',
        backgroundColor: '#FFFFFF',
    })
}

export default styles;