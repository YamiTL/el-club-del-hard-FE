import {style} from 'typestyle';

const styles = {
    container: style({
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
        padding: '10px',
        gap: '0px',
        width: '100%',
        height: '50px',
        backgroundColor: '#725FC9',
    })
}

export default styles;